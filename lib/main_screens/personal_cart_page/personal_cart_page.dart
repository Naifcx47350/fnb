import 'package:flutter/material.dart';

class PersonalCart extends StatelessWidget {
  const PersonalCart({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
